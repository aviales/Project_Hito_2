<%= form_with(model: tweet) do |form| %>
  <div class="field">
    <%= form.text_area :content, class: 'new-tweet' %><br />
  </div>

  <div class="actions">
    <%= form.submit class: 'submit', value: 'Tweet' %>
  </div>
<% end %>
